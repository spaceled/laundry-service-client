import React from 'react';

const ReUse = (props) => {
  return (
    <div>
      <h1>{props.title}</h1>
      <p>{props.paragraph}</p>
      <h2>{props.heading}</h2>
    </div>
  );
};

export default ReUse;